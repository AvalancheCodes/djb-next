/*
 * Contains a scheduling widget that allows users to select a date and time
 * for a consultation.
 * */
import { FC } from "react";

const ScheduleHero: FC = () => {
  return <div></div>;
};

export default ScheduleHero;
